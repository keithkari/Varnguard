#include "VG_PCH.h"
#include "Log.h"
#include <format>

namespace Varnguard
{
	Log::Log()
	{
		
	}

	Log::~Log()
	{
		
	}

	template<typename ...Args>
	inline void Log::info(const char* fmt, Args... args)
	{

	}

	template<typename ...Args>
	inline void Log::warning(const char* fmt, Args... args)
	{
		
	}

	template<typename ...Args>
	inline void Log::error(const char* fmt, Args... args)
	{
		
	}

	template<typename ...Args>
	inline void Log::fatal(const char* fmt, Args... args)
	{
		
	}
}